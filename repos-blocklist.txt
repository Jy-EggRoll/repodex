# Central index blocklist: one owner/repo per line; lines starting with # and blank lines are ignored.
# Empty by default = auto-discover all repositories under the account (archived/disabled ones are skipped automatically).
# Example:
# Jy-EggRoll/some-private-repo
